{
  "name": "graph-docs-cli",
  "version": "0.0.1",
  "main": "dist/graph-docs-cli.cjs.js",
  "bin": {
    "graph-docs": "./bin/bin.js"
  },
  "scripts": {
    "prepare": "husky install && yarn build",
    "lint": "tsc",
    "build": "esbuild src/*.ts --outdir=bin --platform=node --format=cjs && preconstruct build"
  },
  "devDependencies": {
    "@preconstruct/cli": "^2.1.5",
    "@types/node": "^16.0.1",
    "esbuild": "^0.14.21",
    "husky": ">=6",
    "lint-staged": ">=10",
    "prettier": "^2.3.1",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "chokidar": "^3.5.3",
    "front-matter": "^4.0.2",
    "zod": "^3.14.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json}": "prettier --write"
  },
  "author": "Stately Team",
  "license": "MIT"
}
